# ==========================================
# NGOBROL - Pustaka Standar Aksara: percakapan generatif ringan
# ==========================================

impor "aksara.ai.ngobrol" sbg ng

fun jawab(teks) {
    balik ng.jawab(teks)
}

fun klasifikasi(teks) {
    balik ng.klasifikasi(teks)
}